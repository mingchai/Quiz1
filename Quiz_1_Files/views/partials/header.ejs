<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Cluckr 🐔</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <link href="/stylesheets/css/bootstrap.min.css" rel="stylesheet">

        <script src="/scripts/jquery-3.3.1.slim.min.js"></script>
        <script src="/scripts/popper.min.js"></script>
        <script src="/scripts/bootstrap.min.js"></script>
    </head>
    <body>
    <header>
    <nav class="navbar navbar-expand-sm navbar-dark bg-danger">
        <a class="navbar-brand" href="/index"><img src="./images/logo.png" alt="Index" width="30" height="40"></a>

      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
        <div class="btn-toolbar" role="toolbar" aria-label="Toolbar with button groups">
        <div class="btn-group mr-2" role="group" aria-label="First group">
          <form class="form-inline my-2 my-lg-0" action="/cluck" method="GET">
              <button class="btn btn-warning my-2 my-sm-0" type="submit">Cluck!</button>
          </form>
          </div>
        </li>

        <% if (username) { %>
          <span class="navbar-text text-light mr-2">
          Hi, 🍗 <%= username %>!
          </span>
          <form action="/sign_out" method="POST" class="form-inline">
            <button class="btn btn-warning" type="submit">Sign Out</button>
          </form>
        <% } else { %>
          <form action="/sign_in" method="GET" class="form-inline">
            <button class="btn btn-warning" type="submit">Sign In</button>
          </form>
        <% } %>
      </ul>
    </div>

</nav>
    